# QuantConnect Cloud API Credentials
# Get these from: https://www.quantconnect.com/account
QUANTCONNECT_USER_ID=your_user_id_here
QUANTCONNECT_API_TOKEN=your_api_token_here
QUANTCONNECT_BASE_URL=https://www.quantconnect.com/api/v2
QUANTCONNECT_DEFAULT_PROJECT_ID=
QUANTCONNECT_TIMEOUT_SECONDS=300

# Backtest Engine Selection
# Options: "cloud", "local", "auto"
# - cloud: Use QuantConnect Cloud (requires credentials above)
# - local: Use local LEAN Docker engine (free, private)
# - auto: Use local if available, fallback to cloud
BACKTEST_ENGINE=auto
